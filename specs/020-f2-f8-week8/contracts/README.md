# Contracts: F2–F8 Week 8

**N/A** — This feature produces Jupyter notebook deliverables only. There are no API endpoints, services, or inter-component contracts.

Each notebook is self-contained and standalone per Constitution Principle III. The only external interface is the submission query format:

```
0.xxxxxx-0.xxxxxx[-0.xxxxxx...]
```

where each `xᵢ` is specified to 6 decimal places within [0.000000, 0.999999], and the number of hyphen-separated values matches the function dimensionality (F2=2, F3=3, F4=4, F5=4, F6=5, F7=6, F8=8).
