# Contracts: F1 Week 8

No API contracts apply to this feature. The deliverable is a self-contained Jupyter notebook (`f1 - week 8.ipynb`) with no external service interfaces.

**Input contract**: `.npy` files as defined in [data-model.md](../data-model.md).  
**Output contract**: Submission query string in `0.xxxxxx-0.xxxxxx` format.
